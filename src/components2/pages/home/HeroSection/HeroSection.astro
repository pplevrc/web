---
import ScreenFitSection from "@components/layouts/Sections/ScreenFitSection/ScreenFitSection.astro";
import type { ColorThemeBase } from "@content/commons";
import { css } from "@styles/css";
import HeroMovie from "./internals/HeroMovie.astro";
import OverlayPopText from "./internals/OverlayPopText.astro";

interface Props {
  guidelinesShortcut: { title: string; themeColor: ColorThemeBase }[];
}

const { guidelinesShortcut } = Astro.props;

const sectionStyle = css({
  position: "relative",
  overflow: "hidden",
});

const videoStyle = css({
  h: "full",
  w: "full",
  objectFit: "cover",
  zIndex: "base",
});
---

<ScreenFitSection class={sectionStyle}>
  <OverlayPopText guidelinesShortcut={guidelinesShortcut} />
  <HeroMovie class={videoStyle} />
</ScreenFitSection>
