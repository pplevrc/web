---
import Icon from "./Icon.astro";
import type { IconType } from "./types";

interface Props {
  use: IconType[];
}

const { use } = Astro.props;
---

{use.map((type) => <Icon type={type} definition />)}
