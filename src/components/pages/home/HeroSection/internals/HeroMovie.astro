---
import DevHeroMovie from "./DevHeroMovie.astro";
import ProdHeroMovie from "./ProdHeroMovie.astro";

const isDev = process.env["NODE_ENV"] !== "production";
---

{isDev ? <DevHeroMovie /> : <ProdHeroMovie />}
